image: node:16-alpine
pages:
  cache:
    paths:
      - node_modules/
  before_script:
    - apk add git
  script:
    - pnpm i
    - pnpm run build
  artifacts:
    paths:
      - public
  only:
    - main
